<UserControl x:Class="LIC.Malone.Client.Desktop.Controls.HttpStatus"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	mc:Ignorable="d" 
	d:DesignHeight="35" d:DesignWidth="130">
	<UserControl.Template>
		<ControlTemplate>

			<DockPanel Height="35" Visibility="{Binding ControlVisibility, RelativeSource={RelativeSource TemplatedParent}}">
				<Border BorderBrush="{Binding CodeColor, RelativeSource={RelativeSource TemplatedParent}}" BorderThickness="10 1 1 1" VerticalAlignment="Top" Margin="0 0 5 0">
					<Label Foreground="{Binding CodeColor, RelativeSource={RelativeSource TemplatedParent}}" Content="{Binding Code, RelativeSource={RelativeSource TemplatedParent}}"/>
				</Border>
				<Label Name="Description" Foreground="{StaticResource RecededColorBrush}" Margin="0 1 0 0" Content="{Binding Description, RelativeSource={RelativeSource TemplatedParent}}"/>
			</DockPanel>

		</ControlTemplate>
	</UserControl.Template>
</UserControl>